mutation updateUser (
    $id: ID!,
    $firstname: String,
    $lastname: String,
    $nickname: String,
    $phone: String,
    $birthdate: Date,
    $email: String,
    $roles: [ID!]
    $groups: [ID!]
) {
    updateUser(id: $id, input: {
        firstname: $firstname,
        lastname: $lastname,
        nickname: $nickname,
        birthdate: $birthdate,
        phone: $phone,
        email: $email,
        roles: $roles,
        groups: $groups,
    }) {
        id
        firstname
        lastname
        nickname
        birthdate
        created_at
        phone
        email
        roles {
            id
            name
        }
        groups {
            id
            name
        }
        approved
    }
}