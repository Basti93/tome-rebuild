query getCoaches {
    users(
        hasRole: {
            column: NAME,
            value: "athlete"
        }
) {
        id
        firstname
        lastname
        nickname
        imageUrl
    }
}